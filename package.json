{
  "name": "sn-data-migration",
  "version": "1.0.0",
  "description": "ServiceNow data migration & flow migration toolkit — MCP server for Claude Code",
  "type": "module",
  "main": "src/mcp-server.js",
  "scripts": {
    "start": "node src/mcp-server.js",
    "start:http": "MCP_MODE=http node src/mcp-server.js",
    "start:http:port": "MCP_MODE=http MCP_PORT=${PORT:-3000} node src/mcp-server.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "dotenv": "^16.4.5",
    "express": "^4.22.2",
    "zod": "^3.25.76"
  },
  "optionalDependencies": {
    "@napi-rs/keyring": "^1.3.0",
    "@servicenow/sdk-cli": "^4.7.2"
  }
}
