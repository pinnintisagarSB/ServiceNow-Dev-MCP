{
  "name": "servicenow-dev-mcp",
  "version": "2.1.0",
  "description": "ServiceNow Developer MCP — AI-powered toolkit for Claude (116 tools: script generation, code review, portal, catalog, notifications, migration, integrations, and more)",
  "type": "module",
  "main": "src/mcp-server.js",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node src/mcp-server.js",
    "start:http": "MCP_MODE=http node src/mcp-server.js",
    "start:http:port": "MCP_MODE=http MCP_PORT=${PORT:-3000} node src/mcp-server.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "dotenv": "^16.4.5",
    "express": "^4.22.2",
    "zod": "^3.25.76"
  },
  "optionalDependencies": {
    "@napi-rs/keyring": "^1.3.0",
    "@servicenow/sdk-cli": "^4.7.2"
  }
}
